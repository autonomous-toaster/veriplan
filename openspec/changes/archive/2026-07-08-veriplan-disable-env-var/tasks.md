## Phase 1: Implementation

- [x] 1.1 Add `VERIPLAN_DISABLE` check at the top of `main()` in `src/main.rs`, before `Cli::parse()`, with truthy/falsy semantics
- [x] 1.2 Add unit tests for the truthy/falsy parsing logic (9 tests)
- [x] 1.3 Run `veriplan check` on this change to validate the spec (✓ VALID)
